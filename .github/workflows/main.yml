name: C/C++ CI

on: [push]

jobs:
  build:
    name: building
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Hello world action step
      uses: ./.github/workflows # Uses an action in the root directory
      id: hello
      with:
        who-to-greet: 'Mona the Octocat'